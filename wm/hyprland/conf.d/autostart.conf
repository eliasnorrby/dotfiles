exec-once = hyprpaper
exec-once = hypridle
exec-once = waybar
exec-once = swaync

exec-once = systemctl --user start hyprpolkitagent

exec-once = dropbox

exec-once = ~/.local/bin/tmux_set_env

exec-once = ~/.dotfiles/wm/gtk/gsettings.sh

exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# vim: ft=hyprlang
