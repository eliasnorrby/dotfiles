screencopy {
  # Pre-check "Allow a restore token" in the stock picker
  allow_token_by_default = true
  # Auto-answer with the focused monitor; `xdph_share_picker manual`
  # gets the stock picker (window/region) for the next share.
  custom_picker_binary = /home/elias/.local/bin/xdph_share_picker
}

# vim: ft=hyprlang
