---
cicd_config:
  path: dev/cicd
  brew_taps:
    - buildpacks/tap
  brew_formulas:
    - tektoncd-cli
    - terraform
    - terraform-ls
    - tflint
    - pack
    - argocd
  pacman_packages:
    - tekton-cli
    - terraform
    - terraform-ls
    - tflint
    - argocd
  npm_packages:
    - semantic-release-cli
  aur_packages:
    - pack-cli
