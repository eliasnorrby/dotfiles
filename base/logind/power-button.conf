# Power button behaviour. Managed by dotfiles (base/logind).
# Symlinked to /etc/systemd/logind.conf.d/power-button.conf
#
# A short tap is ignored to avoid accidental shutdowns; hold the
# button down to power off cleanly.
[Login]
HandlePowerKey=ignore
HandlePowerKeyLongPress=poweroff
