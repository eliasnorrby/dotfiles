---
work_config:
  path: base/work
  links:
    - src: jira-browse.sh
      dest: "{{ xdg_bin_home }}/"
      rename: jira-browse
      condition: "{{ is_arch }}"
  brew_casks:
    - notion
    - zoom
    - microsoft-teams
    - linear-linear
  aur_packages:
    - teams
    - notion-app
