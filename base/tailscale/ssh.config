# Hosts reachable over Tailscale.
# Managed by dotfiles (base/tailscale), included from ~/.ssh/config.
#
# MagicDNS resolves bare tailnet hostnames through the search domain, so no
# HostName is needed while "Use Tailscale DNS" is enabled.

Host atlas
  User elias
