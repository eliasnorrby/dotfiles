# Settings applied to remote sessions based on $SSH_CLIENT
# Avoid duplicats – only override settings in .tmux.conf

# 2019-02-22 This is currently the only setting not refactored into main conf
# Use another prefix (for nesting)
set -g prefix §

# Use hostname in right status instead of date / time
# set -g status-right "$ts_prefix_indicator$ts_zoomed_indicator $ts_host"

