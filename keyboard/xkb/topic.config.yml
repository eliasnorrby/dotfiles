---
xkb_config:
  path: keyboard/xkb
  # X11-era, kept for reference:
  # pacman_packages:
  #   - xcape
  links:
    - symbols
    - rules
    # X11-era, kept for reference:
    # - setup-keybindings.sh
    # - layout.xkb
