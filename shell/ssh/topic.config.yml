---
ssh_config:
  path: shell/ssh
  links:
    - src: config
      dest: "~/.ssh/"
    - src: macos.config
      dest: "~/.ssh/"
      condition: "{{ is_macos }}"
  brew_formulae:
    - ssh-copy-id
    - mosh
  pacman_packages:
    - mosh
