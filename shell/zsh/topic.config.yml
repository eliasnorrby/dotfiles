---
zsh_config:
  path: shell/zsh
  brew_formulas:
    # essentials
    - zsh
    - fzf
    - bat
    - less
    - ripgrep
    - coreutils
    - watch
    - tree
    - mosh
    - ssh-copy-id
    # extras
    - jq
    - yq
    - pandoc
    # misc
    - neofetch
  links:
    - .zshrc
    - .zshenv
    - src: .zshenv
      dest: ~/.zshenv

zplug_home: "{{ xdg_cache_home }}/zplug"
