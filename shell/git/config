[user]
  email = elias.norrby@gmail.com
  name = Elias Norrby
[core]
  pager = diff-so-fancy | less --tabs=4
[alias]
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  extend = commit --amend --no-edit
  amend = commit --amend
  # 'Remote Prune'
  rp = remote prune origin
  # 'Branch Delete': Delete merged branches
  bdelete = "!f() { git branch --merged | grep -Ev '(\\*|master|main|develop)' | xargs -n 1 git branch -d; }; f"
  # 'Branch Clean'
  bclean = "!git rp && git bdelete"
  # 'Pull Request Delete': Checkout previous branch, deleting the current one
  prd = "!f() { branch=$(git branch --show-current); git checkout -; git branch -D $branch; }; f"
  # 'Quick Save': Create a stash and reapply the changes
  qs = "!f() { git stash push -u -m \"$1\" && git stash apply; }; f"
  # 'Log Remote': Compare current branch to tracking branch
  lr = "!f() { r_branch=\"origin/$(git branch --show-current)\"; git lg ..$r_branch; }; f"
[push]
  default = current
[hub]
  protocol = https
[github]
  user = eliasnorrby
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[pull]
	rebase = true
[merge]
	conflictstyle = diff3

[includeIf "gitdir:work/"]
  path = config-work

[includeIf "gitdir:~/notes/"]
  path = config-notes
