---
git_config:
  path: shell/git
  links:
    - config
    - attributes
    - aliases
    - config-irori
    - config-bemlo
    - config-notes
    - ignore
    - src: gh_browse.sh
      dest: "{{ xdg_bin_home }}/"
      rename: gh_browse
    - src: list_branch_files.sh
      dest: "{{ xdg_bin_home }}/"
      rename: list_branch_files
  brew_formulas:
    - git
    - tig
    - hub
    - gh
    - diff-so-fancy
    - git-delta
    - grip
    # git-flow-avh
  pacman_packages:
    - git
    - tig
    - hub
    - github-cli
    - diff-so-fancy
    - git-delta
  aur_packages:
    - python-grip
